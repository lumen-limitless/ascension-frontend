import {
    JsonRpcSigner,
    Web3Provider,
    JsonRpcProvider,
} from "@ethersproject/providers";

export type SVGProps = {
    className?: string;
};

export type AddressMap = { [chainId: number]: string };
